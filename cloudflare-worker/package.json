{
  "name": "pasay-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Pasay PM Cloudflare Worker: Telegram ingress + Queue consumer + Cron scheduler",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types:src": "tsc --noEmit -p tsconfig.json",
    "types:tests": "tsc --noEmit -p tsconfig.tests.json",
    "types": "npm run types:src && npm run types:tests",
    "test": "tsx --tsconfig tsconfig.tests.json tests/index.spec.ts"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "@types/node": "^22.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "wrangler": "^4.124.0"
  }
}
