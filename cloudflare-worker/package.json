{
  "name": "pasay-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Pasay PM Cloudflare Worker: Telegram ingress + Queue consumer + Cron scheduler",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.28.0"
  }
}
