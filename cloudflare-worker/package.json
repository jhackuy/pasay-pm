{
  "name": "pasay-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Pasay PM Cloudflare Worker: Telegram ingress + Queue consumer + Cron scheduler",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "tsc --noEmit",
    "test": "tsx tests/index.spec.ts"
  },
  "dependencies": {
    "@cloudflare/containers": "^1.20260815.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "@types/node": "^20.11.0",
    "tsx": "^4.16.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.124.0"
  }
}
