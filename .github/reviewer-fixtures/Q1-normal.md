# Reviewer fixture: Q1 NORMAL

This file is a controlled low-risk fixture for Issue #52 reviewer qualification.

Purpose: prove that a clean, low-risk, config-only Markdown documentation change is
not flagged as a blocker by the OpenCode reviewer workflow.

Expected reviewer verdict: `REVIEW_PASS`.

The change is intentionally trivial so the reviewer has no defect to detect and no
contract violation to escalate.
